#include <stdio.h>

int main()
{
    int N = 0;
    scanf("%d", &N);
    
    if(N == 0)
        printf("YONSEI");
    else if(N == 1)
        printf("Leading the Way to the Future");
    
    return 0;
}